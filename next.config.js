// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    appDir: true, // enables app directory (usually default in latest Next.js)
  },
};

module.exports = nextConfig;
